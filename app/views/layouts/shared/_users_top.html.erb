 <!-- Top Navigation Bar -->
<nav class="top-navbar navbar navbar-expand navbar-light d-block d-md-flex">

    <!-- Top Navigation -->
    <ul class="top-nav navbar-nav ml-auto">
        <li class="user-nav-item nav-item dropdown active flex-fill">
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
                <%= link_to 'Edit Profile', users_profile_path, class: 'dropdown-item'%>
                <div class="dropdown-divider"></div>
                <%= link_to 'Log Out', destroy_user_session_path, method: :delete, class: 'dropdown-item'%>
            </div>
        </li>
        <li class="nav-item flex-fill">
            <a class="nav-link" href="<%= users_profile_path %>">
                <%= image_tag('gear-grey.svg') %>
            </a>
        </li>
        <li class="nav-item flex-fill">
            <%= link_to destroy_user_session_path, class:"nav-link", :data => {:confirm => 'Are you sure you want to logout?'}, method: :delete do%>
                <%= image_tag('logout-grey.svg') %>
            <% end %>
        </li>
    </ul>
    <!-- / Top Navigation -->
</nav>
<!-- / Top Navigation Bar -->
